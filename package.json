{
  "name": "flags.games_server",
  "scripts": {
    "start": "bun run src/app.ts",
    "dev": "bun run --hot src/app.ts",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "isomorphic-dompurify": "^2.26.0",
    "nanoid": "^5.1.5",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "ultracite": "5.0.32"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,jsonc,css,scss,md,mdx}": [
      "biome check --apply --staged"
    ]
  }
}